services:

  app:

    build:
      dockerfile: docker/app.dockerfile
    image: abody2k/snake-and-the-ladder
    ports:
      - 80:3000
    environment:
      - SECRET="You smell like carrots!"


  

  redis:
    image: redis

  # frontend:
  #   build: 
  #     dockerfile: docker/frontend.dockerfile
  #   ports:
  #     - 80:3000